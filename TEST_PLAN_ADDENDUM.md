# TinyCal 测试方案补遗：产品风险对账与可执行性判定（草稿）

| 项 | 值 |
|---|---|
| 文档类型 | 测试方案补遗 / 质量评审（基于 [TEST_PLAN_DRAFT.md](TEST_PLAN_DRAFT.md) 增补） |
| 状态 | 草稿（待发布，未关联 PingCode） |
| 仓库 | `TinyCal`，分支 `refactor/code-quality-polish`，HEAD `769bc49` |
| 评审人 | 测试总监 |
| 评审日期 | 2026-08-20 |
| 输入 | 工程师代码基线 + 产品经理《TinyCal 产品风险与需求澄清清单》（消息 7772） |
| 约束 | 不修改生产代码；本阶段产出评审与可执行性判定，不落测试代码 |

---

## 1. 产品风险清单 vs 当前 HEAD 代码对账

> 产品经理清单依据 Memory Wiki 归档结论，部分早于本次重构。测试总监以当前 HEAD 实际代码逐条核对，差异标注如下。

| PM 项 | PM 等级 | PM 依据（归档） | 当前 HEAD 实际代码 | 对账结论 |
|---|---|---|---|---|
| 跨年 `YYYY` 误用 | P0 | `YYYY` 周历年发散 | `Date+Extensions.swift`、`CalendarView.swift`、`TinyCalApp.swift` 全量改为 `yyyy` | **代码已修复**，缺回归测试 |
| `isCruurentMonth` 仅比月 | P0 | 只比 `MM` 不比年 | `Models.swift:27-28` 已改为 `yyyy-MM` | **代码已修复**，缺回归测试 |
| `isActive` 选中碰撞 | （关联） | 历史字符串拼接 | `CalendarView.swift:132-134` 改用 `Calendar.isDate(_:equalTo:toGranularity:.day)` | **代码已修复**，缺回归测试 |
| 缓存无过期/离线降级 | P1 | 无 TTL/版本 | `HolidayViewModel`/`File` 确无 TTL，离线行为未定义 | **未修复**，且期望态待产品决策 |
| 坏缓存永久阻塞回源 | P1（我方） | 解码失败不删 | `HolidayViewModel.swift:13-18` 确认吞错不删 | **未修复**，可立即出证据 |
| 节假日数据源/HTTPS | P0（PM） | 明文 HTTP | `Webservice` 调 `http://timor.tech`；`Info.plist` 设 `NSAllowsArbitraryLoads=true` | **ATS 当前全局放行，HTTP 可用，非系统兼容性回归**；是否迁 HTTPS 属产品范围决策 |
| 菜单栏月份状态 | P1 | 未定义 | `TinyCalApp.onTapBar:103` 每次新建根视图，`@State` 重置为今天 | **当前行为=重置**，可测；"应否保留"待产品决策 |
| 时区/地区 | P1 | 未绑时区 | 全用 `Calendar.current`/`Locale(zh_CN)`，"今天"随系统时区 | **当前=随系统时区**，可测；验收口径待产品决策 |
| `db.swift` 死代码 | P0（PM） | 未入编译 | `project.pbxproj` 引用计数=0，**确未入 Sources**；`FileKit`/`SQLite.xcframework` 仅作文件引用存在 | **确认死代码**；其去留是工程范围决策，**不阻塞测试** |

**核心修正**：PM 列为 P0 的「跨年日期格式」在当前基线**已修复**，不再是阻断；真正待验证的是"缺回归测试"——这正是测试职责。PM 清单中真正仍未解决的 P0 是 **SQLite 死代码去留（工程范围）** 与 **HTTPS 是否纳入本次（产品范围）**，二者均不阻塞测试执行，属范围决策。

---

## 2. 可立即执行 vs 等待产品 P0 决策

### 2.1 可立即执行（不依赖产品决策，待 Test Target 建立后落地）

| 用例 | 覆盖点 | 为何可立即执行 | 关联 PM 项 |
|---|---|---|---|
| TC-1~TC-8 | 日期矩阵/跨年翻月/闰年/月末周日多补整周 | 纯函数 `MyCalendar`/`Date+Extensions`，期望态由算法决定 | PM#3 |
| TC-9~TC-13 | `yyyy` 回归 / `ActiveDay` / `isActive` 跨月同日不误亮 | **回归确认已修复项**，期望态明确 | PM#3（验证已修复） |
| TC-14~TC-23 | 缓存命中/坏缓存/非200/非JSON/`rest`缺失/空响应 | mock `URLProtocol`+`File`，期望态由错误处理决定 | PM#2（部分） |
| TC-24 | 并发同年重复请求 | 并发缺陷暴露，期望态明确（应去重） | — |
| TC-26~TC-29 | 设置开关模板组合/事件触发 | `AppSettings`+`EventBus`，期望态明确 | — |
| **新增 TC-37** | 坏缓存后再次进入应能回源（**当前会失败，暴露 G9 缺陷**） | 缺陷暴露用例，期望态=应回源（与 PM#2 一致） | PM#2 |
| **新增 TC-38** | 跨年缓存命中：次年请求仍命中去年文件 | 当前行为断言（characterization），暴露"跨年无失效"缺陷 | PM#2 |
| **新增 TC-39** | 菜单栏重开重置为今天（当前行为） | 断言**当前**行为（非期望），不依赖产品决策 | PM#4 |
| **新增 TC-40** | "今天"判定随系统时区（当前行为） | 断言**当前**行为=随系统 TZ，可跨时区 mock | PM#5 |

> TC-39/TC-40 为 characterization test（固化当前行为），用于"行为基线"。一旦产品给出"应保留月份/应固定 Asia/Shanghai"决策，再升级为期望态断言。**当前即可编写并运行**，仅记录"现状如此"。

### 2.2 必须等待产品 P0/P1 决策（阻塞"期望态"，不阻塞"现状态"）

| 用例 | 阻塞原因 | 阻塞方 | 解除后的动作 |
|---|---|---|---|
| **缓存过期策略验收** | 按"自然年失效"还是"启动强制刷新"或"带 TTL/版本"？期望态无法确定 | 产品（PM#2） | 给定策略后写期望态断言（如"次年 1 月请求不再命中去年缓存"） |
| **离线降级验收** | 离线时仅显公历无高亮，还是复用旧缓存并提示"可能非最新" | 产品（PM#2） | 给定降级口径后断言 UI/数据态 |
| **HTTPS 迁移验收** | 是否纳入本次重构 | 产品（PM#1） | 若纳入，新增 `https://` 用例 + ATS 关闭后回归；若不纳入，仅记录现状风险 |
| **数据源准确性验收** | timor.tech 调休数据是否可信、是否需备选源 | 产品（PM#1） | 涉及在线 API 与外部数据，仓库内不可自动化；以契约测试或抽样校验处理 |
| **月份状态保留验收（期望态）** | 关闭再开应否保留上次月份 | 产品（PM#4） | 给定后把 TC-39 升级为期望态断言 |
| **时区验收口径** | "今天/节假日"以系统时区还是固定 Asia/Shanghai | 产品（PM#5） | 给定后把 TC-40 升级；若固定时区，新增跨时区用例 |
| **跨年衔接周显示口径** | 上年 12 月与今年 1 月的衔接周如何显示/高亮 | 产品（PM#3） | 给定后补 TC-41 衔接周边界用例 |

### 2.3 SQLite 决策对测试范围的影响

- `db.swift` 经 `project.pbxproj` 引用计数=0 核实为**未编译死代码**，运行时不执行 → **当前无任何测试责任**。
- 其去留是工程重构范围决策（阶段 3），**不阻塞测试执行**，也无需为其编写测试。
- **决策分支**：
  - 若 PM 确认 Roadmap 待办/日程近期规划、保留 DB 层 → 需先入编译、接入 UI，届时再补数据层测试（属未来迭代，不在本批范围）。
  - 若确认无近期规划、清理死代码 → 工程师删除 `db.swift` 与未登记依赖，测试侧仅需在清理后做一次构建回归（确认无引用残留），无功能测试。
- **测试侧结论**：SQLite 决策不进入本测试方案用例集，仅在清理后追加一条"构建无残留引用"回归项（TC-42）。

---

## 3. 更新后的风险分级

| 等级 | 编号 | 项 | 现状 | 处置 |
|---|---|---|---|---|
| P0 | — | — | 跨年日期格式 PM#3 **已在代码修复**；SQLite/HTTPS 为范围决策非阻断 | 当前无测试侧 P0 |
| P1 | G1 | 月末周日多补整周 | 未修复，视觉缺陷 | TC-1 暴露，交工程师修 |
| P1 | G9+G10 | 坏缓存永久阻塞 + 跨年无失效 | 未修复，期望态部分待 PM#2 | TC-16/TC-37/TC-38 暴露，交工程师 |
| P1 | G12 | 失败静默无 UI | 未修复，期望态待 PM#2 | TC-20/21 暴露，UI 口径待产品 |
| P1 | G14 | Cron 闭包可能非主线程 | 未确认，需运行期/线程断言 | TC-34 |
| P2 | HTTPS/ATS | 明文 HTTP + 全局 ATS 放行 | `NSAllowsArbitraryLoads=true` | 安全卫生，是否迁 HTTPS 待 PM#1；非阻断 |
| P2 | G11/G13/G15/G16 | 并发/卡主线程/状态不保留/设置回归 | 见主方案 | 主方案用例 |
| P2 | TZ | "今天"随系统时区，未固定 | 当前可工作但跨时区可能错位 | TC-40 现状断言；验收口径待 PM#5 |

---

## 4. 质量评审结论

**对照产品风险清单的评审结论**：
1. **跨年日期（PM#3，P0）**：代码已在 HEAD `769bc49` 修复（`yyyy` 化 + `isCruurentMonth` 改 `yyyy-MM` + `isActive` 改 `Calendar.isDate`）。**缺陷层面：已修复，不阻断**。测试层面：**零回归保护**，TC-9~TC-13 可立即落地确认修复不回退。无需等待产品决策即可执行。
2. **缓存异常与离线（PM#2）**：坏缓存永久阻塞（G9）与跨年无失效（G10）是**真实未修缺陷**，TC-16/TC-37/TC-38 可立即暴露并出证据交工程师。但"过期策略/离线降级口径"属产品决策，**期望态验收等待 PM#2**。
3. **HTTPS/网络失败（PM#1）**：网络失败/非200/坏JSON 用例（TC-20/21）可立即执行，与 HTTP/HTTPS 无关。**HTTPS 是否纳入本次、数据源是否替换属产品范围决策，验收等待 PM#1**。当前 `NSAllowsArbitraryLoads=true` 使 HTTP 可用，**非系统兼容性即时回归**，但为安全卫生 P2。
4. **月份状态（PM#4）**：当前行为=重置为今天，TC-39 可立即固化现状。**"应否保留"期望态等待 PM#4**。
5. **时区边界（PM#5）**：当前=随系统时区，TC-40 可立即固化现状。**验收口径（系统时区 vs Asia/Shanghai）等待 PM#5**。
6. **SQLite 清理（PM#6，P0）**：确认死代码，**不影响测试范围**；不进用例集，清理后仅加构建回归 TC-42。其去留决策影响**工程重构范围**，不影响测试可执行性。

**综合评审结论**：
- 当前 HEAD 不存在测试侧 P0 阻断。PM 列为 P0 的跨年问题已在代码修复；SQLite/HTTPS 为产品/工程范围决策，不阻塞测试。
- 可立即执行的测试（待 Test Target 建立）：TC-1~TC-29、TC-37~TC-40（共 33 条），覆盖跨年回归、网格边界、缓存坏链、网络失败、设置链、月份/时区现状态。
- 必须等待产品决策方可定期望态的验收：缓存过期与离线降级口径、HTTPS 范围、月份保留、时区口径、跨年衔接周显示。
- **本阶段为方案与评审草稿，不作出最终"通过/不通过"判定**。最终判定待：Test Target 建立 → 第一批可立即执行用例落地运行 → 产品决策收齐后补齐期望态验收 → 汇总。存在 P0 时必为不通过；当前无 P0。

---

## 5. 待确认事项（呈规划师）

1. 请产品就 PM#2 给出缓存过期与离线降级口径，以解锁 TC-37/TC-38 的期望态验收。
2. 请产品就 PM#1 裁定 HTTPS 迁移是否纳入本次，以决定是否新增 HTTPS 用例集。
3. 请产品就 PM#4/PM#5 给出月份保留与时区口径，以升级 TC-39/TC-40 为期望态断言。
4. SQLite 死代码去留由产品/工程裁定（PM#6）；无论哪条分支，测试侧仅追加 TC-42 构建回归，不阻塞。
5. 待规划师确认解除"不运行构建"约束，以建立 Test Target 并落地第一批可立即执行用例。
