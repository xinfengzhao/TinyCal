# TinyCal 测试方案 v2（草稿，含 4 项产品口径期望态）

| 项 | 值 |
|---|---|
| 文档类型 | 测试方案 / 测试矩阵 / 风险分级 / 期望态验收标准 |
| 状态 | 草稿（已纳入 4 项已确认产品口径；待发布，未关联 PingCode） |
| 仓库 | `TinyCal`，分支 `refactor/code-quality-polish`，HEAD `769bc4970943c903a295166c739be3eae4826ba0` |
| 应用类型 | macOS 12+ SwiftUI/AppKit 单 Target 菜单栏应用（`LSUIElement=true`） |
| 评审人 | 测试总监 |
| 评审日期 | 2026-08-20 |
| 约束 | 不修改生产代码；不建立 Test Target；不运行构建。本版为方案与期望态固化 |
| 关联文档 | [TEST_PLAN_ADDENDUM.md](TEST_PLAN_ADDENDUM.md)（产品风险对账，被本版吸收/更新）、[IMPL_ASSESSMENT.md](IMPL_ASSESSMENT.md)（只读技术实施评估） |

---

## 1. 基线与已确认产品口径

**基线确认**：HEAD `769bc49` 未变。`Info.plist` 仍设 `NSAllowsArbitraryLoads=true`；`HolidayViewModel.swift:20` 仍为 `http://timor.tech`；`TinyCalApp.onTapBar:103` 每次点击重建 `NSHostingController(rootView: ContentView(...))`，`@State year/month` 随之重置为今天。

**4 项已确认产品口径（作为期望态验收标准来源）**：

| # | 口径 | 来源 |
|---|---|---|
| C1 | 节假日缓存按**自然年失效**：每年首次请求强制刷新；离线/请求失败时**复用已有缓存**（不清空、不空白）；缓存整体缺失且离线时显纯公历无高亮 | 产品经理（消息末轮） |
| C2 | 本次迁移 **HTTPS**，移除/收紧 `Info.plist` 全局 ATS 放行；数据源仍为 timor.tech，不换源 | 产品经理 |
| C3 | 每次点击菜单栏图标展开**重置为系统当前月**，不保留上次浏览月份 | 产品经理 |
| C4 | "今天"按**系统本地时区**判定；节假日高亮与农历按**固定公历日期**，不做时区换算 | 产品经理 |

**仍未决策**：SQLite/`db.swift` 去留等待用户确认（不影响测试范围，见 §6）。

---

## 2. 现状态 vs 期望态区分（关键）

为避免决策变化导致用例重写，每条用例标注：
- **[现状态]** characterization test——固化当前代码行为，可立即编写运行，断言"现状如此"（非"应如此"）。
- **[期望态]** 验收用例——断言产品口径定义的应然行为。当前代码若已满足则可直接通过；若不满足则**暴露生产缺陷**，交工程师修复后通过。
- **[已修复回归]** 验证 HEAD 已修项不再回退。

---

## 3. 测试矩阵（用例范围）

> 类型：U=单元 / I=集成 / E2E=运行期冒烟。状态列：现状态/期望态/已修复回归。

### 3.1 日期矩阵与跨年/月份边界（核心路径）

| 编号 | 用例 | 输入/条件 | 期望 | 类型 | 状态 |
|---|---|---|---|---|---|
| TC-1 | 月末周日多补整周（缺陷确认） | 构造月末为周日的月份 | 当前补 7 天（缺陷）；修复后补 0 天 | U | 期望态（暴露 G1） |
| TC-2 | 月首各 weekday 落点 | 月首=周一..周日 7 组 | 前置天数正确，首日落在正确列 | U | 期望态 |
| TC-3 | 月末各 weekday 补齐 | 月末=周一..周六 6 组 | 补齐到周末，最后一日落在周日列 | U | 期望态 |
| TC-4 | 跨年翻月 12→1 | `nextMonth` month=12 | year+1, month=1，触发 `populateStocks(year+1)` | U | 期望态 |
| TC-5 | 跨年翻月 1→12 | `preMonth` month=1 | year-1, month=12，触发 `populateStocks(year-1)` | U | 期望态 |
| TC-6 | 2 月闰年 29 天 | 2024-02 | 含 29 个当月日 | U | 期望态 |
| TC-7 | 2 月平年 28 天 | 2023-02 | 含 28 个当月日 | U | 期望态 |
| TC-8 | `getMonthPart` 切片完整 | 任意月 | 内层均长 7，按周分块 | U | 期望态 |
| TC-9 | `yyyy` 回归（非 `YYYY`） | 2024-12-31 / 2025-01-01 | 稳定公历年，不发散 | U | 已修复回归（C 跨年口径） |
| TC-10 | `startOfMonth`/`endOfMonth` 安全解包 | 任意日期 | `?? self` 兜底不崩 | U | 期望态 |
| TC-11 | `Date.parse` 月份补零/非法 | month=1/12/非法 | 合法构造；非法返回 `Date()` | U | 期望态 |
| TC-12 | `ActiveDay.date` 与 `==` | 同日/相邻/跨年同月日 | `==` 仅全等；`isActive` 用 `isDate(...)` | U | 已修复回归 |
| TC-13 | `isActive` 跨月同日不误亮 | active=2025-03-15, cell=2025-04-15 | false | U | 已修复回归 |
| TC-14 | 跨年"当前月"高亮 | 系统日=2026-12-31 翻到 2027-01 / 系统日=2027-01-01 翻到 2026-12 | `isCruurentMonth`(yyyy-MM) 精确匹配，不误判 | U | 已修复回归（C 跨年口径验收①） |
| TC-15 | 跨年衔接周显示 | 上年12月末与次年1月初同周 | 公历日期归属正确（衔接周口径待产品最终确认，先按"日期归属正确"测） | U | 期望态 |

### 3.2 节假日缓存（C1 期望态）

| 编号 | 用例 | 条件 | 期望（C1） | 类型 | 状态 |
|---|---|---|---|---|---|
| TC-16 | 缓存命中不重复回源（现状态） | `stocks[year]` 已有 | return，不发网络 | U | 现状态 |
| TC-17 | 文件命中正常解码 | `readField` 合法 JSON | 解码成功入 `stocks[year]` | I | 现状态 |
| TC-18 | 坏缓存不永久阻塞（C1 相关） | `readField` 截断/非法 JSON | **当前**：吞错不删、永不回源（缺陷）；期望：清理/绕过坏缓存以回源 | I | 期望态（暴露 G9） |
| TC-19 | **自然年失效：年度首次请求强制刷新**（C1） | 同一自然年内已有缓存，跨入新年份首次请求该年 | 期望：发起网络刷新，而非命中旧缓存 | I | 期望态（暴露 G10/无失效） |
| TC-20 | **同自然年内不强制刷新**（C1 语义边界） | 年内多次打开/翻页 | 不反复刷新（避免限流）；具体"年内首次/会话首次"口径需工程落定时再细化 | I | 期望态（待工程口径） |
| TC-21 | **离线/失败复用已有缓存**（C1） | 已有缓存 + 网络失败 | 期望：高亮正常显示，不清空、不空白 | I | 期望态（当前 cache-first 间接满足，但缺"先尝试刷新再回退"语义，交工程） |
| TC-22 | **缓存整体缺失且离线**（C1） | 无缓存 + 离线 | 期望：纯公历视图，无高亮，不崩不空白 | I | 期望态（当前已满足，固化） |
| TC-23 | URL 构造失败兜底 | year 含非法字符 | 打印并 return，不崩 | U | 现状态 |
| TC-24 | UTF-8 编码失败兜底 | mock `String(data:.utf8)=nil` | 打印并 return，不写部分缓存 | U | 现状态 |
| TC-25 | 非 200 响应 | mock 500 | 抛 `NetworkError.invalidResponse`，catch，不更新 | I | 期望态 |
| TC-26 | 非 JSON 响应 | mock 200 非法 body | 解码抛错被 catch，不崩 | I | 期望态 |
| TC-27 | `rest` 可选缺失 | fixture 无 `rest` | 解码成功 | U | 期望态 |
| TC-28 | 空节假日不写缓存 | `holiday.count==0` | 不更新 `stocks`、不写文件 | U | 现状态 |
| TC-29 | 并发同年重复请求 | 两 Task 同时首加载同年 | 期望：去重只发一个请求（当前并发穿透，缺陷） | U | 期望态（暴露 G11） |
| TC-30 | `isHoliday`/`isHolidayWork` 键口径 | year=`yyyy`, day=`MM-dd` | 命中正确 | U | 期望态 |

### 3.3 HTTPS 与 ATS（C2 期望态）

| 编号 | 用例 | 条件 | 期望（C2） | 类型 | 状态 |
|---|---|---|---|---|---|
| TC-31 | 请求走 HTTPS | `HolidayViewModel` 构造 URL | `https://timor.tech/...`，无 `http://` 回退 | U/静态 | 期望态（当前为 http，暴露缺陷） |
| TC-32 | 无明文回退 | mock HTTPS 失败 | 不降级到 http | I | 期望态 |
| TC-33 | ATS 收紧 | `Info.plist` | 移除或收紧 `NSAllowsArbitraryLoads`（不再全局放行） | 静态 | 期望态（当前全局放行，暴露缺陷） |
| TC-34 | HTTPS 证书异常有失败态 | mock 证书错误 | 失败态可观察（与 G12 口径一致），非静默空白 | I | 期望态（依赖 G12 UI 口径） |

### 3.4 菜单栏月份状态（C3 期望态）

| 编号 | 用例 | 条件 | 期望（C3） | 类型 | 状态 |
|---|---|---|---|---|---|
| TC-35 | 重开重置当前月 | 翻页到其他月→关→再开 | 显示系统当前月，非上次浏览月 | E2E | 期望态（**当前代码已满足**，`onTapBar` 重建根视图） |
| TC-36 | 跨天/跨月后重开 | 系统日期跨天后打开 | 反映最新系统当前月 | E2E | 期望态（当前已满足） |

### 3.5 时区判定（C4 期望态）

| 编号 | 用例 | 条件 | 期望（C4） | 类型 | 状态 |
|---|---|---|---|---|---|
| TC-37 | "今天"跟随系统时区 | 系统切非 Asia/Shanghai | "今天"高亮随系统本地日期变化 | U/E2E | 期望态（**当前 `Calendar.current` 已满足**） |
| TC-38 | 节假日不随时区偏移 | 同一公历日期，切时区 | 该日是否法定节假日为固定事实，不偏移 | U | 期望态（当前按 `yyyy/MM-dd` 查表，已满足；固化） |
| TC-39 | 农历按固定公历日期 | 跨时区边界日期 | 农历以公历日期为准，不做即时区换算 | U | 期望态（当前 `Calendar(.chinese)` 用默认 TZ，**边界处可能偏差**，待验证/可能需工程固定 TZ） |
| TC-40 | 跨年边界"今天"不漂移 | UTC 2026-01-01 00:30（上海已 01-01，UTC 仍 12-31 23:30） | "今天"按各自系统时区正确，节假日按固定公历日 | U | 期望态（与 TC-37/38 联动） |

### 3.6 设置刷新链路

| 编号 | 用例 | 条件 | 期望 | 类型 | 状态 |
|---|---|---|---|---|---|
| TC-41 | 三开关全关 | showMd/E/HM 均关 | 显示日历图标, `title=""` | I | 期望态 |
| TC-42 | 开关组合模板 | M+E / M+HM 等 | `toDate(format:tpl)` 正确 | I | 期望态 |
| TC-43 | `bar_conf_change` 触发 | 改菜单栏开关 | 主线程刷新状态栏 | I | 期望态 |
| TC-44 | 农历/假期开关不碰模板 | 改 showChinese/showHoliday | 不触发 `bar_conf_change` | I | 期望态 |

### 3.7 Popover 与状态栏生命周期（运行期）

| 编号 | 用例 | 条件 | 期望 | 类型 | 状态 |
|---|---|---|---|---|---|
| TC-45 | 点外部自动收起 | `.transient`+EventMonitor | 点外自动关并停 monitor | E2E | 期望态 |
| TC-46 | EventMonitor start/stop 配对 | 开/关交替 | 无泄漏/悬挂监听 | E2E | 期望态 |
| TC-47 | 首次自动弹一次 | `didLaunchBefore=false` | 1s 后自动 toggle 并置 flag | E2E | 期望态 |
| TC-48 | Cron 闭包线程边界 | `0 * * * * *` | 主线程更新 `statusItem.button`（当前未约束，疑似缺陷） | I/E2E | 期望态（暴露 G14） |
| TC-49 | 进程常驻无 Dock | 启动后 | `LSUIElement=true`，无 Dock 图标 | E2E | 期望态 |
| TC-50 | 无崩溃日志 | 运行期 | `DiagnosticReports` 无 TinyCal | E2E | 期望态 |

### 3.8 工程卫生（清理后回归）

| 编号 | 用例 | 条件 | 期望 | 类型 | 状态 |
|---|---|---|---|---|---|
| TC-51 | SQLite 清理后无残留引用 | 若决策清理 `db.swift` | 编译无 `db.swift`/`InitDB`/FileKit/SQLite 残留引用 | 构建/静态 | 期望态（**仅当决策为清理时启用**，待用户确认） |

---

## 4. 风险分级（P0/P1/P2）

| 等级 | 编号 | 文件:行 | 风险 | 现状 | 处置 |
|---|---|---|---|---|---|
| P0 | — | — | 当前无测试侧 P0；SQLite/HTTPS 为范围决策非阻断 | — | — |
| P1 | G1 | `Date.swift:30-35` | 月末周日多补整周 | 未修，视觉缺陷 | TC-1 暴露，交工程师 |
| P1 | G9/G10 | `HolidayViewModel.swift:13-18`、`File.swift:45-55` | 坏缓存永久阻塞 + 无自然年失效 | 未修，违反 C1 | TC-18/19/21 暴露，交工程师（含 C1 实现口径） |
| P1 | G12 | `HolidayViewModel.swift:35-37` | 失败静默无 UI | 未修，违反 C1/C2 失败态 | TC-25/26/34 暴露，UI 口径与 C1/C2 一致 |
| P1 | G14 | `TinyCalApp.swift:93-95` | Cron 闭包可能非主线程 | 未确认 | TC-48 线程断言 |
| P1 | C2 | `Webservice`/`Info.plist` | 明文 HTTP + 全局 ATS 放行 | 未修，违反 C2 | TC-31/32/33 暴露，交工程师 |
| P2 | G11/G13 | `HolidayViewModel` | 并发重复/卡主线程 | 见前 | TC-29/性能 |
| P2 | C4-农历 | `Date+Extensions.getChineseDay` | 农历默认 TZ，边界可能偏差 | 待验证 | TC-39；若偏差交工程固定 TZ |
| P2 | G16 | `TinyCalApp.swift:89-91` | 设置回归面 | 见前 | TC-41~44 |

**与既有盘点差异**：C3（菜单栏重置）、C4-"今天"（系统时区）经核对**当前代码已满足**期望态，无需工程师修复，仅需回归用例固化。C1（缓存自然年失效）、C2（HTTPS/ATS）**当前未满足**，需工程师修复后通过。

---

## 5. 可立即执行 vs 待工程/产品解锁

- **可立即编写（现状态 + 已修复回归 + 当前已满足的期望态）**：TC-2~17、TC-22、TC-23~28、TC-30、TC-35~38、TC-40~50（待 Test Target 建立）。其中 TC-35/36/37/38 为"期望态且当前已满足"，可直接断言应然行为。
- **需工程师修复 C1 后通过**：TC-18、TC-19、TC-21、TC-20（口径细化）。
- **需工程师修复 C2 后通过**：TC-31、TC-32、TC-33、TC-34。
- **需工程师修复 G1/G14 后通过**：TC-1、TC-48。
- **待用户确认 SQLite 后启用**：TC-51。
- **待产品最终确认衔接周边界口径**：TC-15（先按"日期归属正确"测）。

---

## 6. SQLite/`db.swift` 对测试方案的影响

- `db.swift` 在 `project.pbxproj` 引用计数=0，确认为未编译死代码，运行时不执行 → **当前无测试责任，不进用例集（除清理后 TC-51 构建回归）**。
- 决策分支：
  - 清理 → 工程师删 `db.swift` + 未登记 FileKit/SQLite 依赖 → 测试侧仅 TC-51 验证无残留引用。
  - 保留 → 需入编译、接入 UI，**属未来迭代**，本批不覆盖。
- **结论**：SQLite 决策不阻塞本方案任何用例的编写与运行（TC-51 仅在"清理"分支启用）。

---

## 7. 待确认/未解决事项

1. C1 自然年失效的工程实现口径（年内首次 vs 会话首次刷新、是否带版本号/TTL）——需工程师落定后细化 TC-20。
2. 跨年衔接周显示口径（TC-15）——待产品最终确认。
3. SQLite/`db.swift` 去留——待用户确认（不阻塞）。
4. 农历时区边界（TC-39）——需运行期/单元验证是否实际偏差。
5. 解除"不运行构建/不建 Test Target"约束——待规划师确认以落地用例。

---

## 8. 结论

- 4 项产品口径已固化为期望态验收标准；现状态与期望态区分清晰，避免决策变化致用例重写。
- **C3（菜单栏重置）、C4-今天（系统时区）当前代码已满足期望态**，仅需回归固化。
- **C1（缓存自然年失效/离线复用）、C2（HTTPS/ATS）当前未满足**，属 P1 生产缺陷，交工程师修复后通过；测试侧提供 TC-18/19/21 与 TC-31~34 的失败用例与定位证据。
- **当前无测试侧 P0**，暂不作出最终"通过/不通过"判定。最终判定待：Test Target 建立 → 可立即执行用例运行 → 工程师修复 C1/C2/G1/G14 → 期望态用例通过 → 汇总。
