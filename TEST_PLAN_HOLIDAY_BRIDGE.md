# TinyCal 假期拼假助手 测试方案（草稿 v1）

| 项 | 值 |
|---|---|
| 文档类型 | 测试方案 / 测试矩阵 / 质量门禁 / P0 锁定结论 |
| 状态 | 草稿（待发布，未关联 PingCode） |
| 仓库 | `TinyCal`，分支 `refactor/code-quality-polish`，HEAD `7eac58bdfc57c2620397a78713c58cc1edd2a426` |
| 功能 | 假期拼假助手（Holiday Bridge Planner） |
| 评审人 | 测试总监 |
| 评审日期 | 2026-08-24 |
| 关联 | PRD REDoc `https://docs.xiaohongshu.com/doc/a732bef7c55b4eb4c3f95eabbbc9cc41`（鉴权受限，正文未独立抓取） |
| 约束 | 暂不修改生产代码；方案确认后再落地测试代码 |

---

## 1. 收到与可读取确认 / 输入完备性

**已收到并可读取**：规划者下发的 PRD 链接、功能要点、已确认代码基线。

**输入来源与证据等级**：
- PRD 正文：⚠️ REDoc 鉴权受限，WebFetch 仅取到标题，**未独立抓取全文**。以下 PRD 要点来自规划者转发的产品经理交付摘要（消息 8155），属二手可靠但非逐字原文。
- 代码基线：✅ 已独立 `git` 核对，HEAD `7eac58b` 与下发一致；生产源码已全量只读复核。

**不缺输入，可启动测试方案**。以下为影响用例精度的**待确认项**（不阻塞本方案，但影响期望态断言的最终形式）：

1. **验收标准逐字原文**：8 条标准仅以主题形式到达（算法正确性/默认列表/自定义输入/高亮联动/数据来源与刷新/空异常态/跨年/权限隐私）。需产品提供逐字口径以固化断言阈值（如"最少请假天数"并列时如何取舍的精确规则）。
2. **并列排序口径**：最少请假天数优先、连续休息最多次之——当两方案"请假天数相同且连续休息相同"时如何 tie-break（起始日最早？假期含周末最多？）待产品/工程定义。
3. **"次年数据未公布"触发条件**：PRD 标注"接口实际行为待工程联调后定义提示触发条件"。需工程联调后定义：是按"当年 12 月仍查不到次年数据"还是"接口返回空 holiday"触发。
4. **年假余额上限**：首期是否支持（影响"自定义输入"用例集是否含余额过滤分支）。
5. **扫描范围与 Top N**：未来 12 个月、Top 3～5 是否可配置（影响性能用例的边界）。

> 按职责：以上属产品/工程口径，不构成"要求补 PRD 才测"。期望行为不清时已具体列出最小确认问题；其余按 PRD 摘要 + 现有产品行为推断。

---

## 2. P0 锁定：日期网格疑似风险（静态验证结论）

规划者要求"先验证或锁定现有日期网格疑似 P0"。本阶段不运行构建，采用**静态逻辑推演**锁定：

### 2.1 G2（表头周一首列 vs weekday Sunday-first 口径错位）→ 误报，非缺陷

`Date.getWeekDay()` 返回 Gregorian `weekday`（1=周日..7=周六）。表头 `lead=["一".."日"]` 为周一首列（Mon=col0..Sun=col6）。月首需补的前置天数 = mondayFirst 列号 = `(weekday+5)%7`。

逐分支核对 `MyCalendar.getMonthDays`（`Date.swift:8-21`）：

| 月首 weekday | 期望前置天数 | 代码分支 | 代码前置天数 | 结论 |
|---|---|---|---|---|
| 1（周日） | 6 | else 分支 `tmp=[1..<7]`，append `-6..-1` | 6 | ✅ 一致 |
| 2（周一） | 0 | `if>1`，`tmp=[1..<1]=[]` | 0 | ✅ 一致 |
| 3（周二） | 1 | `tmp=[1..<2]`，append `-1` | 1 | ✅ 一致 |
| 7（周六） | 5 | `tmp=[1..<6]`，append `-5..-1` | 5 | ✅ 一致 |

`tmp.reversed()` 保证前置日期按时间升序排列。**结论：前置补位口径正确，G2 为误报，锁定为非缺陷**，不阻塞拼假功能。`TEST_INVENTORY_DRAFT.md` 中 G2 风险**下调关闭**。

### 2.2 G1（月末周日多补整周）→ 确认真实缺陷

月末补位（`Date.swift:30-35`）：
```
let _index = endDate.getWeekDay() - 1   // 周日=0..周六=6 = endDate 的 mondayFirst 列号
if _index < 7 {
    for i in [Int](_index + 1 ..< 8) {
        days.append(endDate.addDate(num: i - _index))
    }
}
```
末尾追加天数 = `8 - (_index+1) = 7 - _index`。

| 月末 weekday | 期望末尾补齐到周日列的天数 | 代码追加 | 结论 |
|---|---|---|---|
| 7（周六，_index=6） | 1（次日周日） | 1 | ✅ |
| 2（周一，_index=1） | 6 | 6 | ✅ |
| 1（周日，_index=0） | 0（已在本周末日列） | **7**（多一整周） | ❌ 缺陷 |

**结论：G1 确认为真实缺陷**——月末为周日的月份会多渲染一个完整"下周"行（7 个次月日期）。属视觉缺陷，非数据损坏、非崩溃。`getMonthPart` 按 `((i+1)%7)==0` 切周，总天数仍为 7 的倍数，故多出一行幽灵周。

**对拼假功能的影响**：
- 拼假方案高亮以日期字符串 `yyyy/MM-dd` 查 `HolidayViewModel.stocks`，与网格列位置**解耦**，故 G1 不影响算法判定正确性。
- 但 G1 会在月末为周日的月份多渲染一行**次月日期**；若该次月日期落在拼假方案高亮区间内，会出现"高亮溢出到幽灵周"的视觉不一致。
- **处置**：G1 属生产缺陷，交工程师修复（`Date.swift:30-35` 末尾补齐逻辑：周日应为 0 天，修正 `for i in [Int](_index + 1 ..< 7)` 或对 `_index==0` 不补）。测试侧提供 TC-G1 失败用例作为定位证据，**不自行修改生产代码**。

**风险定级**：G1 = P1（视觉缺陷，非阻断；但作为拼假高亮的前置显示基础，建议在拼假首发前一并修复，避免"高亮溢出幽灵周"被用户当成算法错误）。

---

## 3. 测试策略总览

- **基线区分**：每条用例标注 [现状态] / [期望态] / [已修复回归]，避免 PRD 口径变动导致重写。
- **可测性前置**：拼假算法应为**纯函数**（输入：日期区间 + 节假日/调休/周末数据 + 个人年假/请假偏好；输出：候选方案列表）。纯函数可脱离 UI/网络单测，是本功能验证成本最低的关键。PRD 摘要已确认"可抽象纯函数单测"。
- **依赖注入**：网络（`URLProtocol` mock）、文件缓存（`File` 协议 mock）、时间源（`DateProvider`/`Clock` 协议，注入固定"今天"）须由工程师在落地时抽象；否则跨年/跨天/坏缓存用例不可重复。**可测性改造为测试侧硬性前置**。
- **不引入 EventKit/通知权限**（PRD 首期非目标）→ 不设相关用例。

---

## 4. 测试矩阵

> 类型：U=单元 / I=集成 / E2E=运行期冒烟。状态：现状态/期望态/已修复回归。

### 4.1 拼假算法边界（核心，纯函数单测）

| 编号 | 用例 | 输入/条件 | 期望 | 类型 | 状态 |
|---|---|---|---|---|---|
| HB-1 | 单假期连前后周末 | 1 个法定假期夹在两个周末之间 | 建议请假 0 天即得连续休息；排序第一 | U | 期望态 |
| HB-2 | 调休"班"日排除 | 区间含调休上班日（`isHolidayWork`） | 班日不计入休息，方案不把班日当假期 | U | 期望态 |
| HB-3 | 最少请假优先 | 两方案请假 2 vs 3 天 | 2 天方案排前 | U | 期望态 |
| HB-4 | 请假相同→连续休息最多 | 两方案请假均 2 天，连续休息 5 vs 7 | 7 天排前 | U | 期望态 |
| HB-5 | 并列 tie-break | 请假与连续休息均相同 | 按 PRD 定义口径取舍（待确认项2）；断言稳定可复现 | U | 期望态（待口径） |
| HB-6 | 跨月拼假 | 方案横跨两个月 | 月份边界正确，日期连续不重不漏 | U | 期望态 |
| HB-7 | 跨年拼假 | 方案横跨 12→1 | 年份切换正确，引用两年节假日缓存 | U | 期望态 |
| HB-8 | 闰年 2 月 | 2024-02（29 天） | 区间含 02-29 正确计入；非闰年 2023-02 不含 | U | 期望态 |
| HB-9 | 周末不计入请假 | 区间内纯周末段 | 不需请假即得休息，请假天数为 0 | U | 期望态 |
| HB-10 | 无可行方案 | 区间内无任何假期锚点 | 返回空列表 + 空态文案（见 §4.5） | U | 期望态 |
| HB-11 | 自定义起止/天数 | 用户指定期望天数或区间 | 按用户约束过滤候选 | U | 期望态 |
| HB-12 | 年假余额过滤（条件） | 若首期支持年假上限 | 余额不足方案被过滤；待确认项4 | U | 期望态（条件启用） |
| HB-13 | Top N 截断 | 候选 > N | 仅返回 Top N；待确认项5 | U | 期望态 |
| HB-14 | 算法确定性 | 同输入多次调用 | 结果相同、顺序稳定 | U | 期望态 |
| HB-15 | 大区间性能 | 12 个月扫描 | 在可接受时限内完成（见 §6 性能） | U | 性能 |

### 4.2 数据来源 / 缓存 / 网络失败（复用并扩展节假日链路）

| 编号 | 用例 | 条件 | 期望 | 类型 | 状态 |
|---|---|---|---|---|---|
| HB-16 | 内存命中不回源 | `stocks[year]` 已有 | 不发网络 | U | 现状态 |
| HB-17 | 文件缓存命中正常解码 | 合法 JSON | 解码成功 | I | 现状态 |
| HB-18 | 坏缓存不永久阻塞 | 截断/非法 JSON | 期望：清理/绕过以回源；当前吞错不删（缺陷 G9） | I | 期望态（暴露 G9） |
| HB-19 | 非 200 响应 | mock 500 | 抛 `NetworkError.invalidResponse`，catch，不崩 | I | 期望态 |
| HB-20 | 非 JSON 响应 | mock 200 非法 body | 解码抛错被 catch，不崩 | I | 期望态 |
| HB-21 | `rest` 可选缺失 | fixture 无 `rest` | 解码成功 | U | 期望态 |
| HB-22 | 空 holiday 不写缓存 | `holiday.count==0` | 不更新 `stocks`、不写文件 | U | 现状态 |
| HB-23 | 并发同年重复请求 | 两 Task 同年首加载 | 期望：去重只发一个（当前穿透，缺陷 G11） | U | 期望态（暴露 G11） |
| HB-24 | 手动强制刷新 | 用户触发刷新按钮 | 绕过缓存命中，强制网络拉取并覆写 | I | 期望态（新功能） |
| HB-25 | 刷新失败复用旧缓存 | 已有缓存 + 刷新失败 | 保留旧数据 + 失败态可观察，不空白 | I | 期望态（C1） |

### 4.3 次年数据未公布

| 编号 | 用例 | 条件 | 期望 | 类型 | 状态 |
|---|---|---|---|---|---|
| HB-26 | 次年数据未公布触发 | 当年 12 月仍查不到次年数据 / 接口空 | 展示"次年安排未公布"提示，不崩、不显示错误方案 | I | 期望态（待触发条件，待确认项3） |
| HB-27 | 跨年方案引用次年 | 次年有数据 | 正确使用次年节假日；次年无数据时降级 | I | 期望态 |
| HB-28 | 次年数据后续公布 | 从无到有 | 手动刷新后方案更新 | I | 期望态 |

### 4.4 月历高亮与详情面板

| 编号 | 用例 | 条件 | 期望 | 类型 | 状态 |
|---|---|---|---|---|---|
| HB-29 | 方案高亮联动 | 选中方案 | 月历高亮"休/班/建议请假"三类标记，与算法输出一致 | I/E2E | 期望态 |
| HB-30 | 高亮键口径 | 日期→`yyyy/MM-dd` 查表 | 命中正确，跨月跨年不串 | U | 期望态 |
| HB-31 | 详情面板展示 | 选中方案 | 显示请假天数、连续休息天数、起止日期、含假期名 | E2E | 期望态 |
| HB-32 | G1 幽灵周不污染高亮 | 月末周日的月份 | 拼假高亮不溢出到多渲染的次月幽灵周 | I | 期望态（依赖 G1 修复；修复前为失败用例） |
| HB-33 | 选中日切换重置 | 切方案/切月 | 旧高亮清除，新高亮生效 | E2E | 期望态 |
| HB-34 | 高亮与农历/假期标记共存 | 同一日 | 多类标记不冲突、可读 | E2E | 期望态 |

### 4.5 空 / 异常状态（与 PRD 四类降级对齐）

| 编号 | 用例 | 条件 | 期望 | 类型 | 状态 |
|---|---|---|---|---|---|
| HB-35 | 无网络无缓存 | 首次 + 离线 | 纯公历视图、无高亮、不崩、提示文案 | I | 期望态 |
| HB-36 | 缓存过期但可用 | 跨自然年 + 旧缓存 | 见 C1：强制刷新 + 失败回退（HB-25） | I | 期望态 |
| HB-37 | 无可行方案 | 无假期锚点 | 空态文案 + 不崩 | U/I | 期望态 |
| HB-38 | 次年未公布 | 见 §4.3 | 提示文案 + 不崩 | I | 期望态 |
| HB-39 | 失败态可观察 | 网络/解码失败 | 非静默空白，有数据来源状态（与 G12 口径一致） | I | 期望态（暴露 G12） |

### 4.6 兼容性与回归范围

| 编号 | 用例 | 条件 | 期望 | 类型 | 状态 |
|---|---|---|---|---|---|
| HB-40 | 原月历导航回归 | 拼假功能开关关闭 | 上下月/回到本月/选日高亮行为不变 | E2E | 回归 |
| HB-41 | 节假日标记回归 | 拼假关闭 | 节假日名/班/农历显示不变 | E2E | 回归 |
| HB-42 | 设置链回归 | 菜单栏开关 | `bar_conf_change` 刷新不变 | I | 回归 |
| HB-43 | 状态栏 Cron 回归 | 每分钟刷新 | 主线程更新（G14，待验证） | I | 回归 |
| HB-44 | 深浅色/小尺寸 | 拼假高亮在 dark/light | 可读、不溢出 260px 容器 | E2E | 期望态 |
| HB-45 | 时区一致性 | 系统切非 Asia/Shanghai | "今天"随系统时区；节假日/拼假按固定公历日（C4） | U | 期望态（当前已满足，固化） |
| HB-46 | 跨年"今天"不漂移 | UTC 2026-01-01 00:30 | 按系统时区正确（C4） | U | 期望态 |

### 4.7 可测性基础设施（前置工程改造，测试侧验收）

| 编号 | 用例 | 条件 | 期望 | 类型 | 状态 |
|---|---|---|---|---|---|
| HB-47 | 纯函数可单测 | 算法与 UI/网络解耦 | 不依赖 App 进程即可单测 | U | 前置 |
| HB-48 | 网络可注入 mock | `URLProtocol` | 可模拟 200/非200/坏JSON/超时 | I | 前置 |
| HB-49 | 文件缓存可注入 mock | `File` 协议 | 可模拟命中/坏缓存/写入失败 | U | 前置 |
| HB-50 | 时间源可注入固定"今天" | `Clock`/`DateProvider` | 跨天/跨年用例可重复 | U | 前置 |
| HB-51 | XCTest Target 建立 | 工程 | 单元/集成可运行 | 构建 | 前置 |

---

## 5. 质量门禁

| 门禁 | 标准 | 阻断等级 |
|---|---|---|
| G1 日期网格 | 月末周日不多补整周（TC-G1 通过） | P1，拼假首发前建议修复（HB-32 依赖） |
| 算法纯函数 | HB-1~15 全通过 | P0（核心功能正确性） |
| 缓存/网络失败 | HB-18~25 通过（含 G9/G11 修复或豁免） | P1 |
| 次年未公布 | HB-26~28 通过 | P1 |
| 空异常态 | HB-35~39 通过（含 G12 失败态） | P1 |
| 回归 | HB-40~46 不回退 | P1 |
| 可测性前置 | HB-47~51 落地 | P0（无此前置，自动化覆盖为零，无法回归） |

**结论判定**：存在 P0（算法正确性 + 可测性前置）未满足前，**不通过**。G1 为 P1，不单独阻断，但 HB-32 依赖其修复。

---

## 6. 性能 / 兼容性 / 安全

- **性能**：12 个月扫描应在主线程外完成；`@MainActor` 上不做大规模 JSON 编解码（G13）。HB-15 设时限阈值（待工程定基线）。拼假算法纯函数对 365 天 × 多窗口扫描应 < 100ms（建议值，待确认）。
- **兼容性**：macOS 12+；Intel/Apple Silicon（待产品确认最低版本，待确认项6）。260px 容器布局回归（HB-44）。
- **安全**：PRD 摘要标注 HTTPS 升级为二期。**首期仍为 HTTP + 全局 ATS 放行**（C2 未满足，P1）。测试侧：HB-19~20 覆盖失败态；HTTPS 迁移属二期，本批不设阻断，但建议在拼假首发同期收敛（因拼假依赖网络数据，HTTP 风险被放大）。**待确认项：是否本次一并收敛（产品/工程决策）**。

---

## 7. 分阶段与 MR 验证计划

**阶段 1（本 PRD）**：
1. 工程师落地可测性改造（HB-47~51）+ 修复 G1（TC-G1）+ 纯函数算法。
2. 测试侧编写 HB-1~15（算法）+ HB-47~51（基础设施）→ 运行通过。
3. 工程师落地缓存刷新/失败态（C1 相关）+ 次年未公布 → 测试侧 HB-16~28。
4. 工程师落地 UI 高亮/详情面板 → 测试侧 HB-29~39。
5. 回归 HB-40~46。

**MR 验证计划**：
- MR-1（可测性 + G1 修复 + 纯函数）：跑 HB-1~15、HB-47~51、TC-G1。
- MR-2（缓存/刷新/次年）：跑 HB-16~28、HB-35~39。
- MR-3（UI 高亮/详情）：跑 HB-29~34、HB-40~46。
- 每个 MR 须附实际执行命令与通过/失败结果；测试暴露的生产缺陷交工程师修复，测试侧仅出证据与失败用例。

**阶段 2（后续评审，本批不覆盖）**：年假余额精细化、方案收藏/倒数、HTTPS/ATS 收敛、EventKit 联动。

---

## 8. 未解决 / 待确认事项

1. 验收标准逐字原文（待产品提供）→ 影响 HB-5 tie-break 与阈值断言。
2. 并列排序 tie-break 口径（待产品/工程）→ HB-5。
3. "次年未公布"触发条件（待工程联调）→ HB-26。
4. 年假余额首期是否支持（待产品）→ HB-12 条件启用。
5. 扫描范围/Top N 可配置性（待产品）→ HB-13/HB-15。
6. 最低 macOS 版本/架构范围（待产品）→ 兼容性用例。
7. HTTPS/ATS 是否本次一并收敛（待产品/工程）→ 影响安全门禁是否纳入阶段 1。
8. 解除"暂不修改生产代码"约束 → 待方案确认后落地测试代码与 Test Target。

---

## 9. 结论

- 已收到 PRD 与基线，可启动测试方案；PRD 正文鉴权受限为证据缺口，已具体列出待确认项，不阻塞。
- **P0 锁定**：G2 网格口径错位=误报（下调关闭）；G1 月末周日多补整周=确认真实缺陷（P1，交工程师修复，HB-32 依赖）。
- 测试矩阵 51 条（HB-1~51 + TC-G1），覆盖算法边界/缓存网络/次年未公布/高亮详情/空异常/回归/可测性前置。
- 质量门禁：算法正确性 + 可测性前置为 P0，未满足前不通过；G1 与缓存/失败态为 P1。
- 本方案为草稿，未落盘生产改动；待方案确认后落地测试代码，交工程师评审工程正确性。
